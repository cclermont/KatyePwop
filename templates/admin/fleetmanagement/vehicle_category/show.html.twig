{% extends 'base.html.twig' %}

{% block title %}VehiculeCategory{% endblock %}

{% block body %}
    <h1>VehiculeCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicule_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vehicule_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vehicule_category_index') }}">back to list</a>

    <a href="{{ path('vehicule_category_edit', {'id': vehicule_category.id}) }}">edit</a>

    {{ include('vehicule_category/_delete_form.html.twig') }}
{% endblock %}
