{% extends 'base.html.twig' %}

{% block title %}Type de véhicule{% endblock %}

{% block body %}
    <h1>Type de véhicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicle_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vehicle_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vehicle_category_index') }}">back to list</a>

    <a href="{{ path('vehicle_category_edit', {'id': vehicle_category.id}) }}">edit</a>

    {{ include('vehicle_category/_delete_form.html.twig') }}
{% endblock %}
